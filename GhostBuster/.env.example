# Copy to `.env` — Vite loads `.env` only; `.env.example` is documentation.

# Leave unset during local dev — Vite proxies /api and /compose to the API (port 3001).
# VITE_API_BASE=https://your-api.example.com
